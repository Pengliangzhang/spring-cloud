spring:
  application:
    name: eureka-server-dev
  config:
    import: "configserver:http://localhost:8081"  # 引入 Config Server
  cloud:
    config:
      uri: http://localhost:8081  # Config Server 的 URI
    loadbalancer:
      ribbon:
        enabled: false  # 禁用 Ribbon 负载均衡