spring:
  application:
    name: eureka-server-dev
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: user
    password: password

logging:
  level:
    org.springframework.cloud: DEBUG
