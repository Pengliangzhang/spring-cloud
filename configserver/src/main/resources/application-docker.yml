spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pengliangzhang/spring-cloud.git
          search-paths: configserver/src/main/resources/configs

server:
  port: 8081

logging:
  level:
    org.springframework.cloud: DEBUG

encrypt:
  key: sezpij-2pakvI-qebwoj
