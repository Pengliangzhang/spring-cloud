spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pengliangzhang/spring-cloud.git  # 确保这里是有效的 Git 仓库
          search-paths: configserver/src/main/resources/configs  # 确保路径正确

server:
  port: 8071
